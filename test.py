def mean(numbers):
    a = len(numbers)
    b = sum(numbers)
    return b /    a

c = mean([1, 2, 3, 4, 5,8,9])
print(c)
{"result": c} # array , hashmap
8 # integer
"bataa" # String
[1, 2, 3] # List<intger>
["bataa", "tsogtoo"] # List<string>